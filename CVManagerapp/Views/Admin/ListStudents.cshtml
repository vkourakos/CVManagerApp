@model List<ApplicationUser>

<h1>List of Students</h1>

<table class="table table-bordered table-striped mt-3">
    <thead>
        <tr>
            <th>User Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var student in Model)
        {
            <tr>
                <td>@student.UserName</td>
                <td>
                    <div class="d-flex flex-row gap-2" role="group">
                        <a asp-controller="CV" asp-action="Details" asp-route-userId="@student.Id" class="btn btn-primary">View CV</a>
                        <a asp-controller="CV" asp-action="Create" asp-route-userId="@student.Id" class="btn btn-primary">Create CV</a>
                        <a asp-controller="CV" asp-action="Edit" asp-route-userId="@student.Id" class="btn btn-primary">Edit CV</a>
                        <a asp-controller="CV" asp-action="Delete" asp-route-userId="@student.Id" class="btn btn-primary">Delete CV</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

